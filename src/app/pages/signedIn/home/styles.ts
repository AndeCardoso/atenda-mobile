import { Card } from "@components/base/Card";
import styled from "styled-components/native";

export const Container = styled.View`
  padding-top: 8px;
  padding-bottom: 64px;
`;

export const AdvertiseCard = styled(Card)`
  margin: 0 16px;
`;
